<% title "首页" %>
<%= render "base" %>
<p>欢迎登录中国背包网管理后台</p>
<div class="items">
  <h2>最新添加的公司</h2>
  <ul>
    <% @companies.each do |company| -%>
      <li><%= link_to(company.name, admin_company_path(company), :title => company.name) %></li>
    <% end -%>
  </ul>
</div>
<div class="items">
  <h2>最新添加的栏目</h2>
  <ul>
    <% @areas.each do |area| -%>
      <li><%= link_to(area.name, admin_area_path(area), :title => area.name) %></li>
    <% end -%>
  </ul>
</div>
<div class="items">
  <h2>最新添加的专题</h2>
  <ul>
    <% @topics.each do |topic| -%>
      <li><%= link_to(topic.title, admin_topic_path(topic), :title => topic.title) %></li>
    <% end -%>
  </ul>
</div>
<div class="items">
  <h2>最新添加的文章</h2>
  <ul>
    <% @articles.each do |article| -%>
      <li><%= link_to(article.title, admin_article_path(topic), :title => article.title) %></li>
    <% end -%>
  </ul>
</div>
<div class="items">
  <h2>最新添加的线路</h2>
  <ul>
    <% @routes.each do |route| -%>
      <li><%= link_to(route.title, admin_route_path(route), :title => route.title) %></li>
    <% end -%>
  </ul>
</div>